<!-- Loop over each object in the projects array -->
{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a class="text-color" href="/blog/{{id}}">{{name}}</a>
    </h2>
  </div>
</div>
{{/each}}